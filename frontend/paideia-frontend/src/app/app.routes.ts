import { Routes } from '@angular/router';
import { TestConnectionComponent } from './shared/components/test-connection/test-connection';

export const routes: Routes = [
  { path: '', component: TestConnectionComponent }
];
